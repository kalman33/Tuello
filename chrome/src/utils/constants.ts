/**
 * Constantes partagées de l'extension Tuello
 */

/** Largeur de l'iframe en pixels */
export const IFRAME_WIDTH_PX = 550;

/** Décalage de translation de l'iframe (masquée hors écran) */
export const IFRAME_OFFSET_PX = 570;

/** Délai de debounce en millisecondes (MutationObserver, recherche d'éléments) */
export const DEBOUNCE_DELAY_MS = 300;
